<h1>Hire A Photographer</h1>

<% if @users %>
  <% @users.each do |user| %>
    <%= link_to user.name, user_path(user) %>
    <% if user.photographer %>
      <%= link_to user.name, user_path(user) %>
    <% end %>
  <% end %>
<% end %>
